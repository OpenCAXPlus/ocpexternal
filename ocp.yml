name: external_name
id: external_id
default:
  version: "2.0.0"
  config: build
versions:
  - id: "1.0.0"
    config: [build]
  - id: "2.0.0"
    config: [build]
configs:
  - id: build
    run: source config/default.sh
dependencies:
  - id: dep1
    type: external
    version: ["1.0.0", "2.0.0"]
