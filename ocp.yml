name: external_name
id: external_id
default_version: "2.0.0"
default_config: default
version:
  - id: "1.0.0"
    config: [build]
  - id: "2.0.0"
    config: [build]
config:
  - id: build
    name: CMAKE_BUILD_TYPE
    value: [Debug, Release]
dependency:
  external:
    - id: dep1
      version: ["1.0.0", "2.0.0"]
